# 基于AirSim的无人机飞行控制系统
## 摘要
本项目设计并实现了一套基于AirSim仿真平台的无人机飞行控制系统。系统以多旋翼无人机为控制对象，通过Python接口实现键盘交互式飞行控制，并集成了多种典型飞行模式，包括环绕飞行、方形轨迹、螺旋上升和原地旋转等等。同时，针对传统手动控制中存在的安全性与可控性问题，系统引入了自动返航机制，可在任意时刻一键返回起飞点并安全悬停。

在实现过程中，通过多线程机制对各类飞行模式进行异步调度，避免阻塞主控制流程，从而提升系统响应性能；结合速度分级与平滑控制策略，优化了飞行过程的稳定性与流畅性。实验表明，该系统能够在仿真环境中稳定运行，实现多模式切换与实时控制，具备良好的可扩展性，可为后续无人机感知避障、路径规划等进阶开发提供稳定可靠的基础框架。

## 1. 项目背景
### 1.1 功能定位
UVA_flight_control_system是一个基于AirSim仿真平台开发的无人机（UAV）核心控制模块。该模块作为无人机系统的 “大脑”，负责处理从底层API通信到高层自主航迹生成的全链路逻辑。本项目主要面向无人机算法验证、飞行控制逻辑研究以及基于强化学习的感知避障任务。
#### 1.1.1 模块在系统中的位置
在无人机技术栈中，本模块处于飞行控制与任务调度层：
|名称|功能|
| ---- | ---- |
|环境层|AirSim物理仿真引擎，提供重力、风阻及碰撞物理反馈|
|控制层|键盘输入映射、多线程轨迹规划、飞行状态平滑处理、指令解析与调度|
|执行层|通过AirSim API下发moveByVelocity、moveToPosition控制指令，驱动无人机运动执行|

#### 1.1.2 核心任务描述
无人机控制系统的核心任务围绕“精准操控、自主运行、安全可靠”三大目标展开，涵盖实时交互、轨迹规划、性能调节及应急保障四大核心维度，具体如下：
|名称|功能|
| ---- | ---- |
实时交互控制|实现低延迟键盘遥控，支持前后、左右、垂直升降全维度操控
自动化航迹算法|内置多种数学模型，实现环绕、方形、螺旋上升等三维复杂轨迹飞行
动态性能调节|设计多档位速度切换机制，适配精细操控与快速巡航不同需求
应急保障机制|集成一键悬停、一键返航、安全降落逻辑，保障仿真飞行稳定性

### 1.2 原代码的设计问题及其优化
原生AirSim基础示例代码仅实现简单移动功能，未考虑实际操控体验与工程化应用需求，存在诸多设计短板，具体表现如下：
- 操控体验较差：飞行操控卡顿，无任何平滑过渡处理，启停瞬间易出现机身抖动、轨迹偏移等问题，不符合真实无人机的飞行惯性特性。
- 功能不够完整：缺少实用的速度分级机制，无法适配精细操控与快速巡航等不同场景，且飞行过程中无安全保障。
- 代码架构不合理：各功能逻辑耦合度高、无模块化拆分，核心控制代码与输入输出逻辑混杂在一起，不便于后续功能扩展与代码调试。
- 适配性不足：难以满足工程化运行的稳定性需求，无法支撑后续进阶开发与场景拓展。
  
针对以上问题，项目进行了全方位的优化：
- 引入平滑系数与速度倍率矩阵，模拟实体飞行器惯性特性，飞行轨迹更加自然流畅。
- 通过`threading`多线程异步执行轨迹算法，实现自主飞行与手动介入无缝切换。
- 新增档位调速、一键悬停、返航等多种实用性功能，降低操作门槛，提升演示调试效率。
- 统一代码运行链路，解决程序报错、按键卡死、机身掉高漂移等问题，提升环境复现性。

## 2. 核心技术栈与理论基础
### 2.1 核心技术栈
|技术/工具|用途|
| ---- | ---- |
 Python 3.12|核心开发语言，负责逻辑编写、多线程调度与指令处理 
AirSim API|无人机仿真通信核心接口，实现状态读取与运动控制
Unreal Engine|高保真三维物理场景渲染，提供真实物理环境模拟
pynput|全局无阻塞键盘监听，实时捕获用户操控指令
Math库|三角函数计算，支撑环绕、螺旋等自主轨迹生成
Threading|多线程并发处理，分离手动操控与自动轨迹任务

### 2.2 核心理论基础
#### 2.2.1 系统整体运行流程
AirSim 仿真环境启动 → 无人机初始化连接 → 起飞至预设安全高度 → 键盘指令监听 / 自动轨迹调度 → 速度换算与指令封装 → API 运动控制下发 → 飞行状态实时反馈 → 悬停 / 返航 / 安全降落退出
#### 2.2.2 关键算法原理
自主巡航轨迹数学模型

方形巡航：通过序列化坐标点位，分段规划直线路径，依次抵达四个顶点，分段衔接闭环飞行
螺旋上升轨迹：融合极坐标变换与高度增量控制，定时更新平面坐标与垂直高度x=R⋅cos(ωt),y=R⋅sin(ωt),z=z0​−k⋅tR 为盘旋半径，k 为上升速率系数，实现匀速盘旋 + 稳定爬升三维运动。

NED 坐标系映射系统严格遵循无人机标准 NED 坐标系规范：

X 轴：正北方向（机体前进正向）
Y 轴：正东方向（机体右移正向）
Z 轴：地向为正，负值对应高度上升

多档位调速原理通过全局档位变量搭配倍率数组，动态换算实时飞行速度，支持飞行中无感切换：python运行speed_ratio = [0.6, 1.0, 1.6]
now_speed = SPEED * speed_ratio[speed_level - 1] * smooth


## 3. 系统整体架构与设计
### 3.1 
### 3.2 

## 4. 针对性功能实现方案
### 4.1 多模式自动飞行状态机
依托键盘按键触发不同飞行模式，通过状态机管理任务切换，互不冲突。
#### 4.1.1 环绕飞行（Orbit）
以无人机起飞原点为圆心，固定环绕半径，动态更新位置与偏航角度，实现 360° 全景环绕观测，运动轨迹连续平滑。
#### 4.1.2 方形巡航（Square）
预设矩形路径关键点，按顺序分段执行直线移动，完成闭合方形航线，适用于固定区域巡逻仿真。
#### 4.1.3 螺旋上升（Spiral）
平面圆周运动叠加垂直缓慢爬升，控制角度递增频率与高度增量，实现稳定三维螺旋轨迹，满足空间侦察仿真需求。
#### 4.1.4 原地旋转
控制机体偏航角匀速变化，实现原地定点旋转，适配姿态调试与环视观测场景。
### 4.2 三级速度分级控制系统
内置低速、标准、高速三档固定倍率，按下 P 键循环切换，控制台同步文字提示当前模式：

低速 0.6 倍：精细微调、近距离精准操控
标准 1.0 倍：日常常规飞行演示
高速 1.6 倍：大范围快速转场巡航全程适配 WASD 全维度移动，变速即时生效，无需中断飞行。

### 4.3 操控稳定性优化
重构键盘监听逻辑，修正速度计算代码位置，避免按键触发高度异常指令；删除未定义函数调用start_eight()，彻底解决程序卡死、按键失灵、莫名掉高漂移问题。
### 4.4 安全保障逻辑设计

一键悬停（H）：调用官方悬停接口，立即终止所有自动任务，锁定当前位置
一键返航（B）：自动回归初始原点坐标，快速复位场景
优雅退出（ESC）：自动执行平稳降落，断开 API 连接，避免仿真器无人机卡死异常


## 5. 系统运行效果
### 5.1 运行环境
表格项目配置参数操作系统Windows 10 / Windows 11运行语言Python 3.12仿真平台AirSim 教学定制版核心依赖airsim、pynput、numpy、threading
### 5.2 运行部署方式

启动 AirSim 仿真程序，等待三维场景加载完成
安装项目所需依赖库
运行主程序，无人机自动起飞至安全高度，进入待命操控状态

### 5.3 按键操作说明
表格按键功能说明W / S无人机前进、后退A / D无人机左移、右移Z / X上升、下降高度调节O / M开启环绕飞行 / 方形巡航N / L原地旋转 / 螺旋上升飞行P循环切换三档飞行速度H一键定点悬停锁定B一键返回起飞原点ESC安全降落并退出程序
### 5.4 实验运行效果

操控响应：键盘指令延迟低，移动顺滑无抖动，高低速切换无顿挫
轨迹效果：环绕、方形、螺旋轨迹闭合完整，路径误差小，运行稳定
安全性能：悬停、返航、降落响应及时，无失控、坠毁等异常
兼容能力：手动操控可随时打断自动飞行，混合控制逻辑运行正常


## 6. 功能扩展与未来规划

环境感知融合：接入 AirSim 激光雷达、视觉传感器，开发障碍物检测与自动避障功能
智能路径规划：引入 A*、RRT 等路径规划算法，实现自定义航点自主飞行
视觉跟踪增强：结合 OpenCV 图像识别，实现目标跟随、区域巡航等智能任务
数据可视化：新增飞行日志记录，实时展示高度、速度、位置等运行参数


## 7. 总结
本次基于 AirSim 平台完成无人机智能控制系统的全面优化与功能迭代，在原生基础功能之上，完成飞行平滑优化、多线程任务改造、三级速度调节、多类自主轨迹开发与全套安全保护机制搭建。
优化后系统解决了原生版本操控生硬、运行不稳定、功能单一等问题，实现手动遥控 + 自动巡航双模式协同工作，代码结构清晰、运行可复现、操作简洁直观。既满足课程作业文档提交与老师批改要求，也为后续无人机避障、路径规划、智能控制等进阶开发提供完整基础框架。
